export interface Musique {
    id: number;          // ID unique de la musique
    title: string;       // Titre de la chanson
    artistName: string;  // Nom de l'artiste
    albumId: number;     // ID de l'album auquel la musique appartient
    albumName: string;   // Nom de l'album
    albumImage: string;  // Image de l'album (URL ou chemin de l'image)
  }
  